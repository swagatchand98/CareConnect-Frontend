import React from 'react';
import ProviderRegisterForm from '@/components/auth/ProviderRegisterForm';

export const metadata = {
  title: 'Provider Registration | care-connect',
  description: 'Register as a service provider on our care-connect platform',
};

export default function ProviderRegisterPage() {
  return <ProviderRegisterForm />;
}
